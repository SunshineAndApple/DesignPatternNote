# 作用
一个抽象类公开定义了执行它的方法的方式/模板。它的子类可以按需要重写方法实现，但调用将以抽象类中定义的方式进行。这种类型的设计模式属于行为型模式

# 特性
* 模板模式最标志的特性在于一部分方案要求延迟到子类中，而不是所有方法。这样就把不变的封装到抽象类，将可变的扩展到子类。
也正是由于这个特性，模板模式**只能用抽象类，不能用接口实现**

* 不变的部分必须用**final**修饰

